# cargo-psp EBOOT metadata (read from this crate dir at `cargo psp` time —
# vendor/rust-psp/cargo-psp reads Psp.toml, not [package.metadata.psp]).
# The XMB shows `title` under ICON0; PIC1 is the full-screen backdrop behind
# the listing. assets/ holds committed copies of art/ICON0.png + art/PIC1.png,
# both rendered from the .fig itself by tools/gen-cover.ts.
title = "Pocket Figma"
xmb_icon_png = "assets/ICON0.png"
xmb_background_png = "assets/PIC1.png"
